info:
  name: ADD NEW BOOK
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/books
  body:
    type: json
    data: |-
      {
        "title": "Domain-Driven Design",
        "author": "Eric Evans"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
